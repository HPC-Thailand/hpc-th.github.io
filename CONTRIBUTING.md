# ร่วมส่งข้อมูล / Contributing

ขอบคุณที่ช่วยกันบันทึกประวัติศาสตร์และภูมิทัศน์ HPC ของประเทศไทย 🙏

## ⚠️ ข้อมูลที่ห้ามส่ง

ทุกอย่างใน repo นี้เป็นสาธารณะและอยู่ใน git history ถาวร **ห้ามใส่**:

- รหัสผ่าน, API key, SSH key, token, session cookie, ใบรับรอง
- อีเมลหรือเบอร์โทรส่วนบุคคล — ใช้อีเมลกลางของหน่วยงาน เช่น `hpc@example.ac.th`
- IP ภายใน, hostname ภายใน, ผังเครือข่ายที่ไม่เปิดเผย, กฎ firewall
- ข้อมูลช่องโหว่หรือรายละเอียดเหตุการณ์ด้านความปลอดภัย

หากเผลอส่งความลับเข้ามา ให้ถือว่า **รั่วแล้ว** — revoke/rotate ทันที
แล้วแจ้งผู้ดูแล repo

## วิธีที่ 1 — ฟอร์มบนเว็บ (ง่ายสุด)

ไปที่ <https://hpc.in.th/submit/> กรอกข้อมูล แล้วกดปุ่มสร้าง GitHub issue
ระบบจะเปิด issue ที่กรอกไว้ให้เรียบร้อย — ตรวจทานแล้วกด Submit

## วิธีที่ 2 — Pull Request

### 1. เลือกไฟล์ให้ถูก

| ต้องการเพิ่ม | ไฟล์ |
| --- | --- |
| ระบบ HPC / อัปเดตสเปก | `data/systems.json` |
| หน่วยงาน (องค์กร) | `data/organizations.json` |
| เหตุการณ์ในไทม์ไลน์ | `data/timeline.json` |
| กิจกรรม สัมมนา | `data/events.json` (+ แบนเนอร์ใน `assets/img/events/`) |
| เอกสารอ้างอิง (PDF เก่า) | `reference/docs/` + `data/reference.json` |
| ข้อความบน UI | `data/ui.json` |

### 2. เขียนข้อความสองภาษาเสมอ

ทุก field ที่ผู้ใช้มองเห็นต้องมีทั้ง `th` และ `en`:

```json
"title": { "th": "ซูเปอร์คอมพิวเตอร์เครื่องใหม่", "en": "A new supercomputer" }
```

ถ้าชื่อเฉพาะไม่ต้องแปล ก็ใส่ข้อความเดียวกันทั้งสองภาษาได้

### 3. พิกัดใช้ลำดับ GeoJSON

`"coordinates": [longitude, latitude]` — **ลองจิจูดมาก่อน**
สำหรับประเทศไทย ลองจิจูดอยู่ราว 96–106 และละติจูด 5–21
(ตัว validator จะเตือนถ้าสลับกัน)

### 4. ใส่วันที่ยืนยัน

ถ้าสเปกที่ส่งมาได้รับการยืนยันจากศูนย์เจ้าของระบบ ให้ใส่:

```json
"verifiedOn": "2026-02-14"
```

ระบบที่ไม่มีค่านี้จะขึ้นป้าย "รอการยืนยัน" บนเว็บไซต์

### 5. ตรวจก่อน push

```bash
node tools/validate-data.mjs
python3 -m http.server 8000   # แล้วเปิดดูที่ localhost:8000
```

### 6. เปิด PR

แนบลิงก์อ้างอิง — ข่าว, หน้าเว็บของหน่วยงาน, เอกสาร procurement ที่เปิดเผยได้
หรือระบุว่าเป็นข้อมูลที่ยืนยันโดยทีมวิศวกรของศูนย์เอง

## เพิ่มกิจกรรม

1. เพิ่ม object ใน `data/events.json` — ต้องมี `id`, `title`, `start` เป็นอย่างน้อย
2. แบนเนอร์ (ถ้ามี) วางไว้ที่ `assets/img/events/` แล้วอ้างด้วย `image`
   ย่อให้กว้างไม่เกิน ~1400px ก่อน เพื่อไม่ให้หน้าเว็บหนัก
3. `start` / `end` ใช้รูปแบบ `YYYY-MM-DD` เป็น **ค.ศ.** เสมอ
   เว็บไซต์แปลงเป็น พ.ศ. ให้เองเมื่อแสดงผลภาษาไทย
4. งานที่ผ่านไปแล้วจะย้ายไปหมวด "ที่ผ่านมา" อัตโนมัติ ไม่ต้องลบ

## เพิ่มเอกสารในคลังอ้างอิง

1. วางไฟล์ไว้ใน `reference/docs/` (ชื่อไฟล์เดิมใช้ได้เลย เว้นวรรคและภาษาไทยก็ได้)
2. เพิ่ม object ใน `data/reference.json` โดยใส่ `file` เป็น path จาก root ของไซต์
3. ถ้าไฟล์ใหญ่เกิน 100 MB **จะ push ขึ้น GitHub ไม่ได้** ให้อัปโหลดไว้ที่อื่น
   แล้วใช้ `url` แทน `file` — validator จะเตือนให้เองถ้าลืม
4. ระบุ `pages` และ `bytes` ให้ตรงกับไฟล์จริง (validator เทียบให้)
5. `format` รองรับ `pdf` / `image` / `html` / `link` — ตัวแรกสามอ่านได้ในหน้าเว็บเลย
   ส่วน `link` ใช้คู่กับ `url` สำหรับเอกสารที่โฮสต์อยู่ที่อื่น

## เพิ่มเหตุการณ์ในไทม์ไลน์

`kind` กำหนดหน้าตาของการ์ด:

| `kind` | ใช้เมื่อ |
| --- | --- |
| `era` | แบนเนอร์เปิดยุคใหม่ (ใช้น้อยมาก) |
| `highlight` | เหตุการณ์สำคัญ — การ์ดมีแถบสีส้ม |
| `milestone` | เหตุการณ์ทั่วไป — จุดเล็กบนเส้น |
| `open` | คำถามปลายเปิดท้ายไทม์ไลน์ |

`era` ต้องตรงกับ `id` ใน `eras[]` เช่น `gen4`

**ไม่ต้องใส่ปี พ.ศ.** — เว็บไซต์คำนวณจาก `year + 543` ให้เอง

## แนวทางภาษา

- ภาษาไทย: ใช้ทับศัพท์ที่วงการใช้จริง (cluster, node, cores) ไม่ต้องบัญญัติใหม่
- ภาษาอังกฤษ: ใช้ตัวสะกดแบบ British (organisation, centre) ให้สม่ำเสมอกับที่มีอยู่
- ชื่อหน่วยงาน: ใส่ตัวย่อในวงเล็บครั้งแรก เช่น `มหาวิทยาลัยเกษตรศาสตร์ (KU)`
